#!/usr/bin/env bash

set -euo pipefail

current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [[ "$current_branch" == "main" ]]; then
  echo "[Termalogy] Direct pushes from 'main' are blocked."
  echo "Create a feature branch and open a PR instead."
  exit 1
fi
